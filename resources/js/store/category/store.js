import * as actions from './actions'
import CONSTANTS from '../../core/utils/constants';


const store = {
    state: {
    },

    mutations: {
        
    },
    actions,
    getters: {
    }
};

export default store;
